# AATIF Governance Framework — Python dependencies
# Core engine
numpy>=1.24

# Testing
pytest>=7.0

# Optional: Arabic NLP bridge (not required for tests)
# requests>=2.28
# sentence-transformers>=2.2
